version: "3"

services:
  # db:
  #   container_name: db
  #   image: postgres:latest
  #   restart: always
  #   environment:
  #     - POSTGRES_DB=vuln
  #     - POSTGRES_USER=vuln
  #     - POSTGRES_PASSWORD=vuln
  #   ports:
  #     - 5432:5432

  cve_2022_28346:
    container_name: cve_2022_28346
    restart: always
    build:
      context: ./cve_2022_28346
    volumes:
      - ./cve_2022_28346:/usr/src/app
    ports:
      - 8000:8000
    # depends_on:
    #   - db
    command: |
      bash -c "sleep 5 && python3 manage.py runserver 0.0.0.0:8000"

    # command: |
    #   bash -c "sleep 5 && python3 manage.py migrate && python3 manage.py runserver 0.0.0.0:8000"
